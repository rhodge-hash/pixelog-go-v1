# Production environment variables
NODE_ENV=production
GIN_MODE=release
PORT=8080

# Security settings
CORS_ORIGINS=https://yourdomain.github.io,https://yourdomain.com
MAX_FILE_SIZE=100MB
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60s

# Optional features
ENABLE_METRICS=false
LOG_LEVEL=info